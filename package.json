{
  "name": "tournament-predictor",
  "version": "1.0.0",
  "description": "Tournament Predictor App with React.js frontend and Node.js backend using Google Sheets API",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "setup": "npm run install-all"
  },
  "keywords": ["tournament", "predictor", "react", "node", "google-sheets"],
  "author": "Tournament Predictor Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
